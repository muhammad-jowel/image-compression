import React from 'react';
import Register from '../user/Register';

const RegisterPage = () => {
    return (
        <div>
            <Register/>
        </div>
    );
};

export default RegisterPage;